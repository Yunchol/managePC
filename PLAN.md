# 学童ゲーム時間管理システム 実装計画

## システム構成
- **スタッフPC**: Go サーバー + Web UI をサーブ
- **子どもPC × 6**: Go エージェント（常駐 exe）
- **スタッフのスマホ**: ブラウザで Web UI にアクセス

---

## 実装ステップ

### Step 1: プロジェクト構成を作る
**達成したいこと**: ディレクトリ構成を整えて Go モジュールを初期化する

- [x] ディレクトリ構成を作る（server/ agent/ ui/）
- [x] `go mod init` でモジュール初期化（server・agent それぞれ）
- [x] 依存パッケージ追加（gorilla/websocket）

---

### Step 2: Go サーバー — 基礎
**達成したいこと**: サーバーが起動して WebSocket 接続を受け付けられる

- [x] HTTP サーバーを起動する（ポート 8080）
- [x] WebSocket エンドポイントを作る（`/ws`）
- [x] エージェントが接続してきたら PC 名を登録して管理する
- [x] 接続中の PC 一覧をログに出す

---

### Step 3: Go エージェント — 基礎
**達成したいこと**: 子どもPC 側のエージェントがサーバーに接続して名乗れる

- [x] WebSocket でサーバーに接続する
- [x] 起動時に PC 名（例: `pc-1`）をサーバーに送る
- [x] サーバーとの接続が切れたら自動再接続する
- [x] Windows で単体 exe としてビルドできる（クロスコンパイル）

---

### Step 4: タイマー機能
**達成したいこと**: サーバーからタイマーを送って、エージェントがカウントダウンできる

- [x] サーバー → エージェントへ「◯分タイマー開始」メッセージを送る
- [x] エージェントがカウントダウンを開始する
- [x] 残り時間をサーバーに定期報告する（スタッフ UI に表示するため）
- [x] 残り 5 分・1 分で警告（ダイアログ表示）する
- [x] 一時停止 → PC名と残り時間をサーバーに保存する
- [x] 再開 → 保存した残り時間からカウント再開する
- [x] 日付をまたいだ後に画面を開いたら一時停止データを全リセットする

---

### Step 5: ゲーム終了機能
**達成したいこと**: 時間になったらゲームのプロセスを kill できる＋ブロックモードを追加する

- [x] タイマー終了時に「窓のある全 UI アプリ」を kill する（Chrome・Edge・Roblox・マイクラなど一括対応）
- [x] エージェント自身は kill しない
- [x] 終了完了をサーバーに報告する
- [x] ブロックモード: PC ごとに ON/OFF できる
- [x] ブロック中は 1 秒ごとに監視して UI アプリを即 kill する
- [x] サーバーから「ブロック開始」「ブロック解除」コマンドを受け取る
- [x] `/api/block/start` `/api/block/stop` API をサーバーに追加する

---

### Step 6: Web UI — スタッフ管理画面
**達成したいこと**: スマホから PC ごとにタイマーを設定・確認できる

- [x] 接続中の PC 一覧を表示する
- [x] PC ごとに「15分」「30分」「60分」ボタンを置く
- [x] 残り時間をリアルタイムで表示する（1秒ごとにポーリング）
- [x] 終了したら表示が消える
- [x] スマホで見やすいレイアウトにする
- [x] QR コードを表示してスマホから同じ画面にアクセスできる

---

### Step 7: 再起動検知
**達成したいこと**: 子どもが PC を再起動したらスタッフに通知する

- [x] エージェント起動時にタイマーが稼働中だったか判定する
- [x] タイマー中に再接続してきた場合、残り時間を自動計算してタイマーを継続する
- [x] ブロック状態も再接続時に自動復元する

---

### Step 8: 自動起動設定
**達成したいこと**: PC 起動時に自動でサーバー・エージェントが立ち上がる

- [x] エージェントを Windows サービスとして登録するスクリプトを作る（agent/install.ps1）
- [x] サーバーをスタッフ PC のスタートアップに登録する（server/start.bat）
- [x] Windows 用 exe をクロスコンパイルで生成（server.exe / agent.exe）

---

### Step 9: 動作テスト・調整
**達成したいこと**: 実際の学童環境で問題なく動くことを確認する

- [x] 複数台同時接続のテスト（3台同時で確認）
- [x] タイマー・一時停止・再開・ブロックの動作確認
- [x] 再起動シミュレーション（タイマー継続を確認）
- [x] 再接続後のタイマー自動復元を確認

---

## 現在の進捗
**全 Step 完了**

| Step | 内容 | 状態 |
|------|------|------|
| Step 1 | プロジェクト構成 | ✅ 完了 |
| Step 2 | Go サーバー基礎 | ✅ 完了 |
| Step 3 | Go エージェント基礎 | ✅ 完了 |
| Step 4 | タイマー機能 | ✅ 完了 |
| Step 5 | ゲーム終了機能 | ✅ 完了 |
| Step 6 | Web UI | ✅ 完了 |
| Step 7 | 再起動検知 | ✅ 完了 |
| Step 8 | 自動起動設定 | ✅ 完了 |
| Step 9 | 動作テスト | ✅ 完了 |

## メモ
- サーバー: スタッフPC（LAN 内固定 IP）
- エージェント: クロスコンパイル（Mac で Windows 用 exe を生成）
- スタッフアクセス: `http://192.168.x.x:8080`（同一 Wi-Fi 必須）
- 子どもPC の IP 固定は不要（エージェント側から接続しに行くため）
